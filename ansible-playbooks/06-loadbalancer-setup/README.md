# Load Balancer Setup

Ansible playbook for HAProxy load balancer setup.

## Features

- Install HAProxy
- Configure backends
- SSL termination
- Health checks
- Statistics page

## Usage

```bash
ansible-playbook -i inventory/hosts site.yml
```
