# Database Server Setup

Ansible playbook for PostgreSQL database server setup.

## Features

- Install PostgreSQL
- Configure authentication
- Create databases and users
- Backup configuration
- Performance tuning

## Usage

```bash
ansible-playbook -i inventory/hosts site.yml
```
